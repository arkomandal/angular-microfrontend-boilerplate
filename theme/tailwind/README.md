# Tailwind Theme

Put shared Tailwind theming artifacts here.

Suggested files:

- `tailwind.preset.js`
- `tokens.css`
- `utilities.css`

Current shared entry point:

- `_theme.scss` (imported by host and child apps)
