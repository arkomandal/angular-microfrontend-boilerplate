# Theme Workspace

This folder centralizes reusable theme assets for the host and all child apps.

## Structure

- `angular-material/`: Angular Material theming assets (palettes, typography, density, component themes).
- `tailwind/`: Tailwind shared tokens, presets, and utilities.

Keep this folder framework/theme focused so all projects can consume styles from one place.
